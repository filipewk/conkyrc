## .conkyrc config

configs for `conky`

commands:

    // install
    $ sudo apt install conky-all
    $ cp /etc/conky/conky.conf ~/.conkyrc
    $ gedit ~/.conkyrc

![desktop](/static/conkysetup.png)